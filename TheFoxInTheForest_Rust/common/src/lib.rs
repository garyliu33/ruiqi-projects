pub mod card;
pub mod center_card;
pub mod deck;
pub mod player;
pub mod client_game_state;
pub mod client_move;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
